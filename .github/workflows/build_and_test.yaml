name: "Compile and test"

on:
  push:
    branches: [main]
  pull_request:
    branches: ["*"]

jobs:
  compile_and_test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
      - name: "Compile"
        run: make build
      - name: "Run tests"
        run: make test
